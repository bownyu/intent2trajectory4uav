## 1. 语义契约

- [x] 1.1 在当前生效文档和 OpenSpec artifact 中冻结 `hover` 与 `loiter` 的 Phase 1 站点相对风险语义定义。
- [x] 1.2 将 Phase 1 使用的指标划分为两类：需要给出初始阈值的指标，以及只定义方向的指标。
- [x] 1.3 定义一套后续 penetration 与 retreat 也可复用的共享站点视角指标词汇。

## 2. 配置与校验设计

- [x] 2.1 按照监视型悬停语义重构 `intent_profiles.hover`，不再围绕小尺度几何动作设计。
- [x] 2.2 按照侦察徘徊语义重构 `intent_profiles.loiter`，不再围绕绕飞子型家族设计。
- [x] 2.3 在校验流程中为 `hover` 和 `loiter` 引入共享的站点视角语义检查。
- [x] 2.4 移除 Phase 1 对具名 loiter 几何子型作为语义真值来源的依赖。

## 3. 生成器改造

- [x] 3.1 调整 `hover` 生成逻辑，使其首要目标变为相对站点的稳定监视行为。
- [x] 3.2 调整 `loiter` 生成逻辑，使其首要目标变为不具有持续 penetration 趋势的侦察式站点周边存在。
- [x] 3.3 在 Phase 1 中保持 `hover` 与 `loiter` 在 metadata 和导出结果中的顶层标签不变。

## 4. 验收标准

- [x] 4.1 有效的 `hover` 样本满足 Phase 1 关于低距离漂移、低方位变化和低切向运动的阈值集合。
- [x] 4.2 有效的 `loiter` 样本满足 Phase 1 关于方位变化、切向占优、距离趋势稳定和侦察距离带驻留的阈值集合。
- [x] 4.3 表现出侦察徘徊特征的 `hover` 样本会被拒绝。
- [x] 4.4 表现出持续突防趋势的 `loiter` 样本会被拒绝。
- [x] 4.5 Phase 1 在替换底层语义契约的同时，保持对外意图标签不变。

## 5. 测试项

- [x] 5.1 增加测试，验证 `hover` 与 `loiter` 校验流程可访问共享的站点视角指标。
- [x] 5.2 增加测试，验证有效 `hover` 样本可以通过新的站点相对语义校验。
- [x] 5.3 增加测试，验证有效 `loiter` 样本可以通过新的站点相对语义校验。
- [x] 5.4 增加边界测试，基于站点视角行为而不是几何形状区分 `hover` 与 `loiter`。
- [x] 5.5 增加回归测试，确保 `hover` 与 `loiter` 的标签在样本 metadata 和导出结果中保持稳定。

